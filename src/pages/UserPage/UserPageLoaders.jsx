import { Facebook } from "react-content-loader";

const UserPageLoaders = () => {
	return (
		<div className="mt-5">
			<Facebook style={{ maxWidth: "800px" }} />
		</div>
	);
};

export default UserPageLoaders;
